
= turbo_frame_tag "spending_#{spending.id}" do
  li #{spending.name} #{spending.description} #{spending.amount} #{spending.category} #{button_to 'Edit', edit_spending_path(spending.id), method: :get} #{button_to 'Delete', spending, method: :delete}